rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Firebase is used only for authentication. Application data is stored by
    // the site's authenticated server API, so no browser may access Firestore.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

